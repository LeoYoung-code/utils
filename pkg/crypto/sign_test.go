package encrypt

import (
	"testing"
)

func Test_sign(t *testing.T) {
	// 调用被测方法
	sign()
	// 验证结果
	// 由于 sign 方法没有返回值，所以无法验证结果。
	// 如果 sign 方法有返回值，可以使用 assert 包来验证结果。
	// 例如：
	// assert.Equal(t, expected, actual, "The two words should be the same.")
}
